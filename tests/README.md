# Tests

`core_tests.cpp` is the initial native test executable for deterministic core behavior.

The test surface will expand with P2 to cover:

- execution-mode selection
- Kiwoom response parsing
- order request correlation
- cumulative-fill idempotency
- balance reconciliation
- reconnect recovery state transitions
- emergency liquidation planning
